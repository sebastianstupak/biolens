[package]
name = "biolens-common"
version = "0.0.1"
edition = "2021"
authors = ["Sebastián Stupák"]
description = "Shared data types for biolens"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"], optional = true }

[features]
serialization = ["serde"]
